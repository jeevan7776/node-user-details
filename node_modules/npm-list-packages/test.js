import test from 'ava';
import listPackages from './';

test('should return an array object with a valid username', async t => {
	await listPackages('ceasbz').then(result => {
		t.true(Array.isArray(result));
	});
})
