'use strict';
const axios = require('axios');

module.exports = async username => {
	return await axios.get(`http://r.cnpmjs.org/-/by-user/${username}`)
		.then(response => {
			return response.data[username]
		})
		.catch(error => {});
};
